## “естова€ задача є1

«адача попалась мне при прохождении теста и сразу решить € ее не смог (врем€ тестировани€ было ограничено, мне не хватило времени на все), но запомнил и решил позже.

Ќеобходимо реализовать 

    static bool EqualCount (IEnumerable enumerable1, IEnumerable enumerable2, out int count)

котора€ будет сравнивать елементы _enumerable1_ и _enumerable2_ и в случае совпадающих элементов, 
вернет _true_ а в параметре _count_ будет содержатьс€ количество совпадающих элементов